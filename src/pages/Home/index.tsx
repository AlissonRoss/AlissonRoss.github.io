import React, {FC, Fragment} from "react";
//import NavBar from "";

const Home: FC = (): JSX.Element => { {

      return (
         <section className="homePage">
             <NavBar/>
        </section>
      );
   }
}
export default Home;